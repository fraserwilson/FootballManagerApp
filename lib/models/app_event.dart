class AppEvent {
  final String title;
  final String id;
  final String description;
  final DateTime date;
  final String userId;

  AppEvent(this.title, this.id, this.description, this.date, this.userId);
}
